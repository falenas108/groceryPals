import {StyleSheet} from 'react-native';

const FormStyles = StyleSheet.create({
    formContainer: {
        borderRadius: 20,
        borderWidth: 1,
    },
    text: {
        marginStart: 10,
    },
});

export default FormStyles;
