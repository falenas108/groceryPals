import {StyleSheet} from 'react-native';

const ShoppingViewStyles = StyleSheet.create({
    rootContainer: {
        flex: 1,
        marginHorizontal: 15,
        marginTop: 30,
    },
});

export default ShoppingViewStyles;
